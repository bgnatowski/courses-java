// Uwaga - to jedynie fragment kodu, tego pliku nie da się skompilować w takiej postaci.

catch (ArithmeticException e) {
  System.out.println("Wyjątek: " + e);
  a = 0; // Ustawienie na 0 i kontynuacja działania
}
